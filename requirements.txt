# Runtime dependencies — EXACT pins (source of truth for reproducible builds).
# Mirrors [project.dependencies] in pyproject.toml. See docs/BUILD_GUIDE.md.
#
# NOTE: cadquery + cadquery-ocp pull compiled OpenCASCADE binaries (600 MB+ expanded).
# Confirmed: cadquery-ocp==7.9.3.1.1 ships cp311 win_amd64 wheels (OpenCASCADE 7.7).

cadquery==2.4.0
cadquery-ocp==7.9.3.1.1
pydantic==2.6.4
ollama==0.3.3
PySide6==6.6.3.1
qt-material==2.14
vtk==9.5.2
pyvista==0.48.4
pyvistaqt==0.11.4
ezdxf==1.1.4
trimesh==4.1.8
reportlab==4.1.0
numpy==1.26.4
cryptography==48.0.1
py-machineid==0.6.0
psutil==5.9.8
